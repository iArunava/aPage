<div>
    <h4> Comment by <%= comment.author_name %>
    <p class="comment"><%= comment.body %></p>
    <small>Posted <%= distance_of_time_in_words(Time.now, comment.created_at) %> ago </small>
</div>
